from setuptools import setup, find_packages

setup(
    name = "django-extjs4-forms",
    version = "0.1",
    packages=['django_extjs4_forms'],
    #py_modules = ['forms', 'utils'],
    include_package_data = True,
    zip_safe = False,

    # author metadata
    )
